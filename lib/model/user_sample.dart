class UserSample {
  String nickname = "";
  String userImageUrl = "";
  int totalTodo = 0;
  int completedTodo = 0;

  UserSample(this.nickname, this.userImageUrl, this.totalTodo, this.completedTodo);

}